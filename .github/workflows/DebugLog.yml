name: Debug log
on: [push]
jobs:
  log:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "::group::Show emvirionment variables"
          printenv
          echo "::endgroup::"
      - run: |
          echo "::debug::Debug"